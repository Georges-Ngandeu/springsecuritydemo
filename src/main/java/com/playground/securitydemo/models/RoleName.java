package com.playground.securitydemo.models;

// This is an enumeration (enum), which is a special data type that enables for a variable to be a set of predefined constants.
public enum RoleName {
    USER, // The USER constant represents a regular user role.
    ADMIN; // The ADMIN constant represents an administrative user role.
}